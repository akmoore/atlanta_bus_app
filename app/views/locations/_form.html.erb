<%= form_with(model: location, local: true) do |form| %>
  <% if location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label 'Address or Approximate Location' %>
    <%= form.text_field :street_address, id: :location_street_address, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :city %>
    <%# form.text_field :city, id: :location_city, class: 'form-control' %>
    <%# form.collection_select :city, cities, :to_s, :to_s, {},  class: 'form-control'%>
    <%# form.select :city, Location::MARTA_CITIES, {}, class: 'form-control'%>
    <%= form.select :city, cities, {}, class: 'form-control'%>
  </div>

  <div>
  <p>
    <%= location %>
  </p></div>

  <div class="actions">
    <%= form.submit 'Find My Bus!', class: 'btn btn-default' %>
  </div>
<% end %>
